source "https://rubygems.org"

gemspec

# Force CI/dependency resolution to use released mlx gem, not local submodule gemspecs.
gem "mlx", ">= 0.30.7.5", "< 1.0"

# Use local mlx-ruby submodule during development.
# gem "mlx", path: "mlx-ruby"
